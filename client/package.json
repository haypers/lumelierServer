{
  "name": "lumelier-client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node -e \"require('fs').mkdirSync('public',{recursive:true}); require('fs').copyFileSync('../branding/favicon.svg','public/favicon.svg')\" && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.6",
    "vite": "~6.0"
  }
}
